interface:
  display_name: "Source Slice"
  short_description: "Run one source through the RAG pipeline"
  default_prompt: "Use $slice to run or resume one source through the RAG pipeline."
policy:
  allow_implicit_invocation: false
