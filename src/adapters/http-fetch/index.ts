/**
 * HTTP fetch adapter — the concrete Fetcher port over global fetch.
 * Constructed only by src/main.ts. See docs/architecture.md §4.
 */
export { HttpFetcher, type HttpFetcherOptions } from "./http-fetcher.js";
